import VoltageDivider from "./6_VoltageDivider"

/// @declare(true)
/// module VoltageDivider {
///   @side("left")
///   analog vin = (3)
///   @side("right")
///   analog vout = (4)
///   @side("bottom")
///   analog gnd = (5)
/// }
/// @declare(true)
/// module VoltageDivider {
///   @side("left")
///   analog vin = (6)
///   @side("right")
///   analog vout = (7)
///   @side("bottom")
///   analog gnd = (8)
/// }
/// module TwoVoltageDividers {
///   net vin = (0)
///   net vout = (1)
///   net gnd = (2)
///   cell vd1 = VoltageDivider() {vin=(0), vout=(1), gnd=(2)}
///   cell vd2 = VoltageDivider() {vin=(0), vout=(1), gnd=(2)}
/// }
module TwoVoltageDividers {
  net vin, vout, gnd
  cell vd1 = VoltageDivider {*}
  cell vd2 = VoltageDivider {*}
}
